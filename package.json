{
  "name": "minary-agent",
  "version": "0.0.1",
  "description": "autonomous AI agent framework — self-improving runtime with built-in learning loops, multi-chain execution, 40+ tools.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "eslint src/",
    "format": "prettier --write ."
  },
  "dependencies": {
    "pino": "^9.5.0"
  }
}